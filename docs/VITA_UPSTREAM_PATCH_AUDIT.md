# Upstream patch accounting for the Vita port

**This audit is in progress. Inventory coverage is not proof that every patch has been ported or every omission is safe.**

Source baseline: `487932a84143c66a74c16cd79388ef4dadf68569`. The default upstream patch build contains **162 replacement functions**; one additional source definition is disabled in that build.

The Vita build returns before `PatchesLib` and patch registration. Its runtime uses the ROM-generated functions, `us.vita.toml` hooks, native helpers and the reduced RT64 backend. Each row below accounts for a source replacement; a partial port can carry a gameplay correction while retaining original graphics or menu behavior.

## Coverage

| Disposition | Definitions |
|---|---:|
| equivalent | 12 |
| partial | 14 |
| retained_original | 136 |
| upstream_disabled | 1 |

A `review_required` or `missing` row is an open porting issue. `retained_original` records an intentional decision for the current renderer/features, with its proof boundary in the evidence column. `partial` must not be read as full equivalence.

## Source and build verification

`python3 tools/audit_vita_patches.py --check` checks source/reference-header hashes, definition coverage and complete accounting fields. It does not run gameplay tests. After building the upstream MIPS patch objects with `patches/Makefile`, add `--objects` to compare the actual `.recomp_patch` function symbols. After strict patch recompilation, `--recompiled OUTPUT_DIRECTORY` checks the generated registration metadata. `--require-resolved` additionally fails while any review or missing-port rows remain. Passing that option confirms a recorded disposition for every function; it does not resolve partial integrations or certify runtime correctness.

The inventory was checked against actual MIPS objects, a linked patch ELF, and strict patch recompilation. All 162 names appear in the generated registration table. Original behavior was reviewed against reference C where usable and against generated MIPS for nonmatching or ambiguous references. The evidence column records the method and validation limits for each function. Inventory/build checks do not establish runtime equivalence.

The newer upstream commit `b414b88e0e632674e1d9a075bd24bd61fb58803f` adds desktop UI callback execution. It is outside the pinned source baseline and does not explain the earlier missing audio fix.

## Function accounting

### patches/boot_logos_patches.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_menu_8003292C](../patches/boot_logos_patches.c#L36) | Adapt the Dolby Surround licensing card from 480i to 240p | equivalent: Vita carries upstream's 240p logo rectangle size, doubled texture-coordinate derivatives and three printText positions/scales. The original texture and string requests, four I4 tile loads and fade timer remain. The desktop extended-GBI enable, extended-RDRAM and rectangle-aspect commands are omitted for the classic Fast path at 4:3. The upstream Rare-logo comment names the preceding animated logo; a targeted host capture identifies texture 0xB0 as the Dolby Surround card. | Nine instruction replacements and four hooks in us.vita.toml adapt the actual generated entry. Its Fast F3DEX2/GLES regression fails on the old logo sampling and legal-text arguments, then passes across fade boundaries and batching modes. The rendered logo is 102 by 40 pixels, matching upstream's two 20-pixel tile rows; its centering uses the nominal 41-pixel half-height. Commands and asset/text callback arguments match compiled upstream in 2304 cases covering every fade byte and nine font-metric combinations after removing only the four desktop extended commands. The font callbacks are modeled, so this does not prove native glyph appearance. A targeted host GLES capture at graphics task 280 shows the actual Dolby logo and all three text lines at the corrected layout, followed by the DK Rap. The quiet Vita3K/Vulkan run also reaches the Rap, but its timed screenshots miss this card. | Validate the Dolby card on physical Vita, including text readability and fade timing; keep the 240p mode selection and matching layout together. |
| [func_global_asm_805FBFF4](../patches/boot_logos_patches.c#L198) | Present the boot logo with aligned sound/timing and add desktop per-frame integration | partial: After the original startup timer receive, the Vita hook waits cooperatively for the existing VI callback to unblank the CPU logo, queues the original sound with private guest arguments, and holds logo mode for 1.4 seconds using the runtime clock. It retains the original main-loop dispatch, 4:3 scissor data and CPU framebuffer path. Desktop GPU logo commands, viewport rewriting, interpolation state and frontend/mod events are not imported. | Full 175-instruction original loop plus upstream source comparison. Before/after host traces show logo mode was previously reset before the 60-VI unblank point, while the fixed run captures the red logo and proceeds to the intro (261 graphics tasks and 339 audio buffers over 12 seconds). dk64_boot_logo_checks verifies ordering, duration, cancellation, clock wrap, private arguments and both floating-register alias modes. The quiet native Vulkan build reaches the intro with unchanged saves/log. Its desktop viewport block moves the four letterbox-bound writes out of 805FB944 and refreshes them each frame from the frontend border setting (0 or 40), with a forced 40 for selected Helm intro cutscenes. The Vita retains the original initializer as their provider; the paired setup and original-consumer checks in the 805FB944 row demonstrate why these changes must stay together. | Validate physical boot visuals/audibility and broader main-loop modes; native screenshot timing did not capture the red logo itself. |
| [func_global_asm_8060E1A8](../patches/boot_logos_patches.c#L384) | Notify desktop events after EEPROM initialization | retained_original: The queue setup retains the same guest addresses/counts; the added behavior is recomp_on_eeprom_load. Vita uses the original EEPROM initialization and native save backend without desktop event exports. | Reviewed the full replacement body, its named feature/event calls, and the current Vita control/build path. No new function-specific gameplay test is claimed. | Revisit when adding the corresponding frontend feature or event API; retain explicit control/save regression coverage. |
| [func_global_asm_805FB7E4](../patches/boot_logos_patches.c#L396) | Prepare the CPU Nintendo logo and copy it to a separate desktop GPU texture | retained_original: Vita retains the original decompression, framebuffer clear and centered 192x48 pixel copy. Its CPU-only VI presentation path makes the extra redNintendoLogoTexture allocation/copy unnecessary. The missing display was caused by startup leaving logo mode too early, corrected at the main-loop handoff. | Compared the complete matching reference C and all 88 generated instructions. A real host boot produced 5278 colored framebuffer pixels but remained VI-blank before the handoff fix. With the fix, a captured CPU-only frame visibly shows the red Nintendo logo before any graphics task. The quiet ARM VPK then reaches the normal intro in Vita3K/Vulkan; its short red-logo window was not captured there. | Confirm red-logo presentation and sound on physical Vita; the direct logo-image capture currently proves the host path. |

### patches/patches_balancing.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_806BE8BC](../patches/patches_balancing.c#L127) | Compensate rabbit race speed for lag | partial: The native helper applies lag/2.1 to the original double speed product before the integer movement argument is formed. | Generated hook at 0x806BEBD0, before trunc.w.d; dk64_timing_checks covers two/three/six-frame lag and fractional bonus speed. | Validate the rabbit race itself; arithmetic/call-site checks do not establish the complete minigame experience. |
| [func_bonus_80029364](../patches/patches_balancing.c#L434) | Compensate Krazy Kong Klamour timers for lag | partial: Vita carries the 162-frame initial timer, lag decrement, -81 reset boundary and 129/81 difficulty durations. The original 30/48 byte is retained and expanded at its timer read into the existing signed 16-bit game-state field, preserving the default 50 and adjacent overlay data. | Original bonus code has four byte stores and one timer read for the difficulty duration. The actual Vita-generated handler (including its hooks) and compiled upstream replacement match in 2946 paired cases: 18 initialization cases across all four difficulties plus defaults, 2016 timer/fade cases, 864 input and win/loss-state cases, and 48 inactive-phase cases. Actor/AAD/game/input state, semantic helper arguments, real game RNG state, expanded duration and relocated sound counters match; 448 round resets produce complete six-target permutations. The test records the original 80029b9c and upstream 8081f4fc render callbacks by their shared function identity; their rendering difference is covered separately in that callback's row. MIPS-II/o32 layout assertions cover the actor fields, game data and two-level target-coordinate pointer layout. The comparison passes AddressSanitizer and preserves the bytes adjoining the original duration. Graphics, audio, text/action and hit-result callbacks are modeled, so this is handler-logic evidence rather than minigame gameplay proof. Evidence: build/upstream-klamour-review/validation.json. Native ARM practice runs now clear all four difficulties in Linux Vita3K 4074-496939b6 with Vulkan, double-buffer mapping and default CPU optimizations. Requirements 10/15/5/10 fall to zero; initial timer 162 and round timers 129/129/81/81 are observed; every run enters control state 2 and returns to Snide HQ in Adventure mode. The test uses original Snide entry 80712774 and only supplies controller inputs: banana aiming, one shot per appearance and neutral-stick reloads. Across the final runs, logs record 40 target decrements, eight reloads and no penalties. See docs/VITA_KLAMOUR_VALIDATION.md and build/vita3k-linux-control/klamour-*-default-cpu/result/validation.json. This establishes practice-mode behavior in that emulator profile, not physical Vita or normal barrel entry. | Validate normal bonus-barrel access and physical Vita controls, visuals, audio and gameplay. All four practice difficulties now have native hit, win and return evidence; first-read framebuffer limitations remain separate. |

### patches/patches_debug.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [raiseException](../patches/patches_debug.c#L34) | Guest exception reporting and forced diagnostic failure | retained_original: The requested clean Vita package removes diagnostic output. It retains the guest exception path and the native runtime error handler rather than importing this printf/crash replacement. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Keep failure reporting useful in diagnostic builds; do not treat normal gameplay tests as exception-path validation. |
| [func_global_asm_805FE71C](../patches/patches_debug.c#L122) | Guest exception diagnostics | upstream_disabled: The default upstream preprocessing disables this diagnostic display replacement; it is not in the compiled patch bundle. | Absent from the MIPS .recomp_patch symbols while its source definition remains in patches_debug.c. | Reassess if the upstream diagnostic build condition changes. |

### patches/patches_draw.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_80630DCC](../patches/patches_draw.c#L194) | Apply a desktop minimum draw distance to actor visibility and opacity | retained_original: The added actorDrawFilter/recomp_filter_draw calls raise the visibility and distance-based opacity thresholds according to the desktop setting. Vita retains each actor's original authored distance and the original culling, lighting, opacity and model submission paths. | Compared the original 536 instructions and compiled upstream function with its real filter helpers. With the desktop floor set to zero, all 432 cases match actor properties/opacity, ordinary display-list words and consumed helper values exactly across actor flags/types, camera distances, multiplayer, frame bank, bone sampling and cull results. Enabling a 20000-unit floor changes the expected positive-control actor case. Culling, pose/lighting, bone and model-draw helpers are modeled. | Validate camera-distance transitions in representative maps; revisit the additional filters only when exposing a wider draw-distance option. |
| [func_global_asm_806303C4](../patches/patches_draw.c#L354) | Apply the desktop draw-distance floor to chunk/prop records | retained_original: The replacement adds recomp_filter_draw to the incoming distance. Vita retains original draw distances and the original record allocation; the malloc/_malloc spelling change does not represent a different allocator in the generated Vita function. | Full reference-C comparison plus generated call 0x806304B0 to the original _malloc at 0x80610FE8. recomp_filter_draw returns the maximum of the original distance and the configured desktop floor. No patch-specific prop-distance playthrough is claimed. | Keep the original distance path while extended draw distance is disabled; revisit with its allocation/culling dependencies if that feature is added. |
| [func_global_asm_80689F80](../patches/patches_draw.c#L433) | Frontend-controlled actor/enemy draw distances | retained_original: Vita has no draw-distance override frontend. The original distance tests remain in force. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80689FEC](../patches/patches_draw.c#L438) | Frontend-controlled actor/enemy draw distances | retained_original: Vita has no draw-distance override frontend. The original distance tests remain in force. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068A074](../patches/patches_draw.c#L444) | Frontend-controlled actor/enemy draw distances | retained_original: Vita has no draw-distance override frontend. The original distance tests remain in force. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068A118](../patches/patches_draw.c#L451) | Frontend-controlled actor/enemy draw distances | retained_original: Vita has no draw-distance override frontend. The original distance tests remain in force. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068A164](../patches/patches_draw.c#L457) | Frontend-controlled actor/enemy draw distances | retained_original: Vita has no draw-distance override frontend. The original distance tests remain in force. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80727958](../patches/patches_draw.c#L530) | Frontend-controlled actor/enemy draw distances | retained_original: Vita has no draw-distance override frontend. The original distance tests remain in force. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8066AF40](../patches/patches_draw.c#L690) | Drain the expanded deferred-decompression array | retained_original: The original drain stays paired with the retained 192-entry producer and storage. It frees compressed inputs through the original decoder wrapper and releases its shared workspace after draining. | dk64_decompression_queue_checks executes the generated drain and ownership wrapper: all 192 completions are consumed, 96 compressed inputs and one workspace are released exactly once, and all returned outputs remain live. | Keep producer, capacities and cleanup paired; extend actual-game queue-pressure coverage when adding new content paths. |
| [getPointerTableFile](../patches/patches_draw.c#L721) | Asset override callback and expansion of deferred-loading capacity | retained_original: Vita retains the complete original 192-entry job/DMA/completion-queue system. The upstream 1024-entry expansion and mod asset override callback are not selected with the current original asset/draw-distance paths. | The actual generated producer/drain/decoder-ownership routines pass 192 mixed jobs, cache/direct-pointer paths, immediate loading and the 193rd-job guard. A 120 s host adventure run observed 98 pending jobs at peak across 5638 asset requests; transport/codec are modeled in the focused fixture. | Reassess capacity together with all three storage arrays if asset overrides or longer draw distances are enabled; later dense-map stress remains useful. |

### patches/patches_framebuffer.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_805FE398](../patches/patches_framebuffer.c#L95) | Execute the desktop GPU snapshot overlay before clearing the scene depth buffer | retained_original: The sole added call renders overlay layer 0, introduced by the desktop rdpStoreFB replacement for CPU snapshots. Vita synchronizes and copies the original framebuffer instead, so it does not register that GPU snapshot callback. The original depth-clear commands are retained. | Full reference-C diff plus the layer selector/reset in func_global_asm_8068C20C and the producer in patched func_global_asm_8070A848. All 70 generated direct overlay registrations use layers 1, 3, 5 or 7. dk64_zipper_checks exercises the generated snapshot caller/copy and verifies one capture per initialization; it models the renderer synchronization callback. | Revisit together with snapshot registration if GPU overlay capture is introduced; physical snapshot/readback validation remains open in the 8070A848 row. |
| [func_global_asm_8070A848](../patches/patches_framebuffer.c#L109) | Make rendered framebuffer data available to CPU effects | partial: Vita retains the original copy/morph and synchronizes its source through the graphics queue. This differs from upstream GPU-overlay snapshots. | Host readback/feedback tests and pause-image checks. The native Linux ARM probe with Vita3K 4074-496939b6, Vulkan and double-buffer mapping records only game-requested bytes: first reads contain a stale Nintendo logo or black pixels; a second pause at the same address returns the room image and visibly produces the original blurred background, followed by resume. Disabled mapping remains black in the renderer control. See docs/VITA_READBACK_VALIDATION.md for raw-capture counts, native screenshots and proof limits. | Validate first-read correctness, other CPU framebuffer effects and physical-Vita copies, including the requested speedhack variant; the repeated-pause result does not resolve those limits. |
| [func_global_asm_80629300](../patches/patches_framebuffer.c#L135) | Preserve fractional half-lag for framebuffer transition timing; adapt snapshots and effect rendering | partial: Vita hooks retain the original mul.s half-lag before integer truncation and carry it to all eight progress consumers. Integer fades/wipes truncate after arithmetic; radial/clock effects retain their double updates. The original 4:3 CPU framebuffer allocation, alpha, pause blur and two-frame deferred release remain; desktop stored_framebuffer, widescreen and interpolation commands are not imported. | Compared reference C, upstream replacement and original generated instructions at 0x80629348, 0x80629730, 0x806298F8, 0x80629A94, 0x80629D48, 0x80629D50, 0x80629DC0, 0x80629EE8 and 0x8062A010. dk64_framebuffer_effect_checks executes the generated routine: odd/even lags, integer truncation after subtraction, sampled lag, all seven transition boundaries, deferred release, pause exit, one-time matrix initialization and guest register/stack preservation. It failed before the hooks and passes with them. Graphics callbacks are stubbed, so this does not validate GPU pixels. Separately, the ARM game probe on Linux Vita3K/Vulkan with double-buffer mapping completes two pause/resume cycles in DK's house: the first capture is black, while the second at the same address contains 72030 colored pixels and produces the blurred room background in Vita3K's native screenshot. This validates repeated pause rendering in that scene, not all transition branches; see docs/VITA_READBACK_VALIDATION.md. | Validate affected fades/wipes, first pause captures and physical Vita snapshots, including the requested speedhack behavior. The first-read emulator failure remains open despite the successful repeated pause. |
| [func_global_asm_8070A934](../patches/patches_framebuffer.c#L378) | Drive zipper scene handoff, snapshot selection and resource cleanup | retained_original: The desktop changes clear the overlay list after its GPU capture, use stored_framebuffer and add interpolation matrix tags. Vita retains its allocated CPU snapshot and original overlay lifetime. Original MIPS confirms that initialization already falls through into rendering in the same call; the reference pseudocode misleadingly nests that work inside a switch case. | Compared all 208 original instructions with the upstream body, including the branch convergence at 0x8070AA20. dk64_zipper_checks executes this driver plus actual 8070AF24 initialization and 8070A848 copying: frame-bank advance, one capture/allocation pair, requested map/exit, five deferred frees, model release, prior-mode restoration, countdown, completed-frame reuse and unchanged overlay count. Mesh rendering, map loading and resource parsing are modeled. | Validate the complete closing/opening zipper during physical gameplay; this fixture proves the driver and ownership paths, not model animation or GPU snapshot pixels. |
| [func_global_asm_8070B05C](../patches/patches_framebuffer.c#L451) | Disable CPU changes to the zipper snapshot for the desktop GPU capture path | retained_original: The empty desktop replacement belongs to its GPU snapshot/alpha-ignoring renderer. The original routine modifies only RGBA16 alpha in six center columns of the allocated CPU snapshot to form a zipper seam. Vita keeps this meaningful pixel effect and the texture-alpha combiner. | Original 171-instruction routine, caller guard at 0x8070BCA4..0x8070BCBC (ticks 5 through 85), and the 24 halfword masks at VRAM 0x80755074 (decompressed ROM 0x02159D74 using the .global_asm mapping). dk64_zipper_checks compares every pixel across eight boundary/intermediate ticks with both alpha parities, RGB/neighbors and buffer guards. Replacing only the test-extracted routine with the upstream-style no-op fails; restoring it passes. | Validate the visible seam through a complete physical-Vita zipper transition; keep this CPU mutation, copy ownership and alpha mode together if changing snapshot strategy. |
| [func_global_asm_8070AC74](../patches/patches_framebuffer.c#L540) | Expand the zipper display-list scissor and fill to the desktop full-screen bounds | retained_original: The complete reference-C diff changes only the original 10,10..309,229 scissor to full dimensions and the fill endpoints from width/height minus one to width/height. Vita keeps the original fixed 320x240 4:3 presentation and overscan region; the segment, projection, color/depth setup and display-list ownership are unchanged. | Compared the complete matching reference C in code_10E1D0.c with the patch and checked the fixed 4:3 fast-renderer presentation. The zipper driver regression models this display-list builder and does not claim a rendered border test. | Inspect zipper borders on physical Vita; revisit these viewport choices if adding a widescreen presentation mode. |
| [func_global_asm_80706F90](../patches/patches_framebuffer.c#L575) | Expand scrolling menu-background tiling/scissor and filter the desktop expanded-aspect image | retained_original: Original MIPS already advances X/Y offsets at 0x80754F70/0x80754F74 by four and wraps at 512; the new menuXShift/menuYShift storage is not a missing animation fix. Its four-by-three repeats of a 128-pixel pattern cover the original 320x240 view across the scroll range. Retain the original point filter/scissor and cached eight textures. Desktop changes add extra tiling, extended-scissor commands and bilinear filtering for the expanded-aspect menu issue. | Inspected the original 479 instructions and compared 20 offset/wrap/vertical-shift cases with compiled upstream: scroll updates and eight texture loads agree; the original emits 96 texture rectangles versus 240 upstream at 320x240. Commit a8037cf changes only the filter and names issue https://github.com/Rainchus/Donkey-Kong-64-Recompiled/issues/42, whose report concerns Expanded aspect ratio. This comparison does not establish every menu pixel. | Check submenu scrolling at the native 4:3 view; revisit tiling/scissor/filter as a unit if expanded aspect ratio is added. |
| [func_global_asm_80709BC4](../patches/patches_framebuffer.c#L702) | Ignore snapshot alpha and initialize primitive white for the desktop zipper renderer | retained_original: The new MODULATEI_PRIM combiner ignores texture alpha and therefore also requires the added white primitive color. The original caller supplies DECALRGBA, which uses texture RGB/alpha directly and ignores stale primitive color. Vita retains it for the CPU-generated alpha seam; importing the desktop pair would erase that effect. | Compared the 407 original instructions and upstream mesh/texture command construction. Original caller 8070B7EC emits FCFFFFFF/FFFCF279 with mode 0x00553078 at 0x8070BAF4..0x8070BB1C. rt64_gl_checks renders those exact modes with stale green primitive/shade colors, alpha 0/31/32/255, and batching off/on; transparent samples preserve the background and visible samples remain untinted. Original CPU seam checks also pass. Mesh animation and later-map appearance are not established by this focused GL check. | Validate complete zipper geometry and seam appearance on physical Vita; keep the alpha-ignoring combiner paired only with a deliberately changed snapshot path. |
| [func_global_asm_80704B20](../patches/patches_framebuffer.c#L786) | Tag skybox color-blend matrices for desktop interpolation and aspect handling | retained_original: The Vita renderer has no interpolated matrix history and uses the original fixed 4:3 projection. Retain the original sky colors, view/environment tint, underwater darkening, transform inputs and eight-vertex draw path without the desktop matrix-group commands. | Compared original generated MIPS and the compiled upstream replacement with their own ROM/patch constants loaded. All 64 cases (eight styles, both banks, tint lookup/override, darkening and interpolation flags) match scalar helper inputs, matrix results and all 24 ordinary display-list commands after the added matrix tags are removed. Math/rotation/tint helpers are modeled; this is not a rendered scene test. The 64 paired cases were rerun with the actual generated cosine routine replacing the earlier trig model; the comparison result is unchanged. | Validate representative sky colors and underwater views in gameplay; revisit the omitted tags if renderer interpolation or aspect modes are added. |
| [func_global_asm_807069A4](../patches/patches_framebuffer.c#L913) | Scale textured skybox transforms for desktop aspect ratios and tag interpolation | retained_original: The added horizontal scale is width*3/(height*4), equal to one at Vita's fixed 4:3 view. Retain the original texture loading, pan/wrap/split calculations, rotation and tile drawing; desktop matrix-group tags have no consumer in this renderer. | Original and compiled-upstream generated routines matched in all 64 fixed-4:3 comparison cases after matrix tags were removed: both texture-cache states and banks, positive/negative pan, zoom/wrap inputs, helper arguments, matrix results and ordinary commands. UI dimensions, matrix helpers, texture splitting and draws are modeled, so the actual race/bonus sky pixels are not established. The 64 paired cases were rerun with the actual generated cosine routine replacing the earlier trig model; the comparison result is unchanged. | Validate Aztec Beetle Race and bonus backgrounds in gameplay; recompute horizontal coverage if a wider aspect is introduced. |
| [func_global_asm_80707980](../patches/patches_framebuffer.c#L1008) | Always draw Galleon sky and extend desktop fallback-background fill endpoints | partial: A single instruction patch removes the optional-sky rejection at 0x80707B5C, carrying the upstream Galleon correction for chunks 0, 6, 7 and 8 while retaining style/view tint. Other maps retain their original dispatch and fill endpoints: this backend already implements fill-cycle inclusive lower-right bounds, so the desktop endpoint extension is not imported. | Compared original and compiled upstream dispatch across 216 map IDs and seven chunk samples (1512 cases). Beyond the explicit one-pixel fill endpoint extension, the only consumed-helper differences were the four Galleon sky omissions. dk64_skybox_checks fails before the instruction patch and passes afterward, covering 252 Galleon chunk/override/view combinations, caller state and representative Japes, Beetle Race and fallback backgrounds. Render helpers are modeled. | Validate affected Galleon sky regions on physical Vita and in an actual scene run; the generated-code regression proves sky dispatch/arguments, not final pixels. |

### patches/patches_heap.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_80631FAC](../patches/patches_heap.c#L10) | Expanded static prop storage | retained_original: Vita retains the original map-dependent prop allocation; it does not import the desktop draw-distance/prop-capacity expansion. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Stress-test dense maps and preserve original allocation capacity until a larger budget is justified. |
| [func_global_asm_80600D50](../patches/patches_heap.c#L132) | Relocate the audio engine into static desktop patch storage | retained_original: The original guest heap and bank loading remain available on Vita. The separately required audio notification fix is ported in the scheduler. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80610350](../patches/patches_heap.c#L276) | Move the work-heap slot into desktop static storage | retained_original: Vita keeps the original heap allocation and registration together. The desktop replacement substitutes heap_slot[0x40000] for the original allocation of the selected work-heap size plus 0x80. The original framebuffer/depth layout, fixed pools and 16-byte object headers remain paired on Vita. | Compared the complete replacement with generated MIPS: framebuffer alignment at 0x806104E4..0x80610514, depth top 0x805FAE00, pool sizes 0x3810/0xC010/0x2810/0x61620 and preserved-object compaction agree. Original 0x806108C8..0x806108E4 allocates size+0x80 and registers it. Host Training Grounds/map loads and extended native attract mode exercise this path; no dedicated preserved-object compaction test is claimed. | Retain the complete original allocation system; reassess its budget if desktop display-list or draw-distance expansions are introduced, and extend dense-map/heap-pressure validation. |
| [func_global_asm_8060A1B0](../patches/patches_heap.c#L506) | Static music scratch storage and music-load event callback | retained_original: The original code already subtracts adjacent music-table offsets and rounds odd byte counts up. Vita retains its paired 0x8000-byte scratch allocation/free and original player buffers; the desktop static scratch buffer and mod callback are not imported. | Generated instructions 0x8060A2CC..0x8060A2F8 confirm the same size calculation. dk64_music_loading_checks exercises four channels, even/odd spans, loader flags, sequence setup, scratch release and guest-stack restoration. | Measure stop/poll timing and decoded audio fidelity separately; this ownership/size check does not resolve audio underruns. |
| [func_global_asm_806291B4](../patches/patches_heap.c#L553) | Capture the framebuffer used by transition rendering | partial: Original allocation/copy calls reach the synchronized func_global_asm_8070A848 hook; the upstream no-allocation GPU-overlay path is not used. | Source call-site inspection and host pause/transition readback checks; physical result not established. | Test transition capture with original allocations and representative map changes. |
| [func_global_asm_806FFF88](../patches/patches_heap.c#L595) | Read the rendered framebuffer for a fairy photograph | partial: A hook synchronizes the selected framebuffer before the original CPU photo loop. The original allocation and texture-only fallback remain; desktop static photo storage is not imported. | dk64_photo_capture_checks uses the actual generated capture/tile loops. It failed before the hook and passes for both framebuffer selections, 160x128 center tiles, allocation ownership, fallback and guest stack. | Validate an actual fairy photograph in gameplay and on physical Vita; the fixture isolates color conversion and does not resolve Vita3K CPU readback limitations. |
| [func_global_asm_80699284](../patches/patches_heap.c#L657) | Fairy-photo actor lifetime with desktop static capture storage | retained_original: Vita retains the original photo allocation and matching actor cleanup. Importing the desktop removal of both release paths would leak the heap-backed Vita photograph or its cached-image fallback. | Compared the full replacement against reference pseudocode and the generated cleanup at 0x80699850/0x80699860, which calls the texture-cache release or deferred free. The generated capture fixture verifies original heap ownership and the actor-217 fallback; it does not execute the complete photo-actor lifecycle. | Validate complete photograph creation, display and deletion in gameplay; keep the two release paths paired with their original producers. |
| [func_global_asm_8061138C](../patches/patches_heap.c#L756) | Warn when the deferred-free budget is exceeded | retained_original: The replacement adds diagnostic reporting; logging is deliberately absent from the clean Vita package. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Review allocation-pressure behavior independently; omission of a warning does not establish that the budget cannot be exceeded. |

### patches/patches_interpolation.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_8061DBD4](../patches/patches_interpolation.c#L86) | Suppress desktop perspective interpolation at camera segment starts and cutscene completion | retained_original: Retain original camera tracking, cutscene command processing, lag arithmetic and state updates at the native game rate. Upstream adds a perspective-interpolation timer reset: value 2 at segment initialization and value 3 at completion, preserving any larger existing timer. Vita has no interpolated intermediate camera frames. | The original 937-instruction routine and compiled upstream replacement match the first 8 MiB of guest memory and gameplay-helper arguments in 493 paired cases: 80 normal-camera/control cases, 336 cutscene command/duration/phase cases, 45 flag combinations and 32 timer-boundary cases. The actual generated camera-bank advancement and trig routines run; physics, tracking, timing and other callbacks are modeled. The comparison was rerun after replacing the earlier trig model with the actual game routines, with the same result. Upstream-only interpolation storage and the temporary guest stacks are outside the compared region. Timer tests verify initialization/completion values and preservation of an existing value 5. | Validate complete camera cuts, segment transitions and return to player control during native and physical gameplay. The controlled callback comparison does not execute full camera collision or complete cutscene playback. |
| [func_global_asm_80620F00](../patches/patches_interpolation.c#L409) | Suppress perspective interpolation during camera recovery | retained_original: The extra lockdown protects interpolation history around camera changes. The reduced renderer does not maintain that history. | Reviewed the full replacement body, its named feature/event calls, and the current Vita control/build path. No new function-specific gameplay test is claimed. | Revisit when adding the corresponding frontend feature or event API; retain explicit control/save regression coverage. |
| [func_global_asm_8068A508](../patches/patches_interpolation.c#L482) | Disable two distance-dependent model load/unload cases | retained_original: The removed cases swap alternate actor models by distance. Vita has no actor interpolation history and retains the original model loading and unloading behavior. | Patch/reference or generated-source inspection plus the fixed 4:3 presentation in rt64_fast_vitagl.cpp. No dedicated later-map visibility test is claimed. | Validate representative camera edges and model-distance changes; reconsider if widescreen or interpolation is added. |
| [func_menu_8002FC1C](../patches/patches_interpolation.c#L542) | Reset interpolation around transitions and actor warps | retained_original: The additional locks protect desktop interpolation history, which the current Vita renderer does not maintain. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806ABC94](../patches/patches_interpolation.c#L588) | Disable desktop interpolation after a pause-page slide completes | retained_original: The original pause switcher already handles navigation priority, page wrap, alternating page buffers, twenty-unit slide steps, completion flags and sound events. Upstream adds a two-frame interpolation lockdown on completion. Vita keeps the original switcher because the renderer has no interpolated intermediate frames. | The original 106-instruction routine and compiled upstream replacement match exactly in 864 paired cases: both active page buffers, three page-count limits, first/last pages, simultaneous/opposite/no navigation buttons and nine positive/negative slide offsets around the clamp and completion boundaries. Checks cover the full pause state record with guards, return value, both page visibility flags, the existing completion-state byte and redraw/sound arguments. Drawing and sound callbacks are modeled; upstream-only interpolation-timer storage is excluded. | Validate pause-page changes and final page placement in native gameplay and on physical Vita. Keep this navigation decision separate from the pause-drawing, framebuffer-readback and HUD patches still under review. |
| [func_global_asm_80724B5C](../patches/patches_interpolation.c#L687) | Reset interpolation around transitions and actor warps | retained_original: The additional locks protect desktop interpolation history, which the current Vita renderer does not maintain. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80613194](../patches/patches_interpolation.c#L712) | Reset interpolation around transitions and actor warps | retained_original: The additional locks protect desktop interpolation history, which the current Vita renderer does not maintain. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806131D4](../patches/patches_interpolation.c#L720) | Reset interpolation around transitions and actor warps | retained_original: The additional locks protect desktop interpolation history, which the current Vita renderer does not maintain. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80613214](../patches/patches_interpolation.c#L728) | Reset interpolation around transitions and actor warps | retained_original: The additional locks protect desktop interpolation history, which the current Vita renderer does not maintain. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80727678](../patches/patches_interpolation.c#L753) | Reset interpolation around transitions and actor warps | retained_original: The additional locks protect desktop interpolation history, which the current Vita renderer does not maintain. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806BFBF4](../patches/patches_interpolation.c#L954) | Suppress desktop actor interpolation after large scripted position changes | retained_original: Retain original cutscene-actor event dispatch, movement, animation and emitter behavior. Upstream records the old position during scripted movement states 14/15 and requests actor interpolation lockdown value 2 when any resulting coordinate changes by more than 100 units. The extra lockdown belongs to desktop interpolation state. | The original 1323-instruction routine and compiled upstream replacement match the first 8 MiB of guest memory and gameplay-helper arguments in 604 paired cases: 448 event/initialization/map cases, 144 actor-state cases and 12 movement-boundary cases. Tests include event codes 0..26, Rap/non-Rap handling, rotation/scale updates, allocation/free arguments, sprite emitters and movement completion. The real generated game RNG and rand wrapper run, with final RNG state included in the comparison. Physics, animation, allocation and drawing helpers are modeled; only the added interpolation callback is excluded. Both signs just below, exactly at and above 100 verify the strict displacement threshold. | Validate actor motion, emitters and cutscene exits in representative native/physical scenes. This comparison covers controlled helper behavior rather than animation assets, physical movement or rendered interpolation. |
| [func_global_asm_8061C518](../patches/patches_interpolation.c#L1313) | Reset cutscene-bar interpolation | retained_original: Original cutscene bars are retained; no interpolated bar history is active on Vita. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8061C600](../patches/patches_interpolation.c#L1329) | Reset cutscene-bar interpolation | retained_original: Original cutscene bars are retained; no interpolated bar history is active on Vita. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [playCutscene](../patches/patches_interpolation.c#L1370) | Remove lingering cutscene controllers before spawning another | partial: The native hook removes actor type 173 after the rejection guard and before spawnActor. It also rechecks a slot after immediate deletion compacts the actor list. Desktop interpolation/events are not imported. | MIPS actor/table layout assertions and dk64_gameplay_fix_checks cover deferred/immediate deletion, moved-tail controllers, empty lists and caller registers. The compaction case failed before the loop correction. | Validate repeated Owl Race/cutscene attempts in gameplay; helper tests do not establish that scenario. |
| [func_global_asm_806F386C](../patches/patches_interpolation.c#L1474) | Notify desktop autowalk events and reset cutscene-bar interpolation | retained_original: Vita retains the original autowalk setup. The additions reset desktop perspective interpolation when cutscene bordering is enabled and emit recomp_on_autowalk; neither facility is connected to the current Vita renderer/runtime. | Full reference-C diff and helper inspection. Generated 0x806F38F8 uses the same 8-byte item stride and 0x806F3914 reads the item pointer at offset 4, confirming that the renamed items field preserves the original layout. Opening-story/Training Grounds autowalk has run in host validation; no interpolation or desktop callback equivalence is claimed. | Validate further autowalk/cutscene sequences; import callback/interpolation behavior only with those corresponding features. |

### patches/patches_lightning.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_8068AD7C](../patches/patches_lightning.c#L145) | Expose configurable lightning intensity | retained_original: Vita retains the original lightning intensity and sound behavior, with no desktop intensity setting. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806443E4](../patches/patches_lightning.c#L420) | Expose configurable lightning intensity | retained_original: Vita retains the original lightning intensity and sound behavior, with no desktop intensity setting. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |

### patches/patches_main.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_dk64_boot_8000102C](../patches/patches_main.c#L15) | ROM DMA and native overlay mapping | equivalent: The ROM-DMA wrapper and load_dk64_overlay mapping replace desktop ROM/address handling without importing the desktop patch binary. | platform/vita/game_support.cpp and us.vita.toml; native boot, intro and earlier map-transition checks in docs/VITA.md. | Keep overlay mapping and native ROM writes covered when adding later-map validation. |
| [func_dk64_boot_80000450](../patches/patches_main.c#L34) | ROM DMA and native overlay mapping | equivalent: The ROM-DMA wrapper and load_dk64_overlay mapping replace desktop ROM/address handling without importing the desktop patch binary. | platform/vita/game_support.cpp and us.vita.toml; native boot, intro and earlier map-transition checks in docs/VITA.md. | Keep overlay mapping and native ROM writes covered when adding later-map validation. |
| [func_global_asm_80611730](../patches/patches_main.c#L50) | Remove an uncached corruption check while retaining deferred frees | equivalent: The single uncached load is replaced with the expected comparison value. The seed call and original deferred-free countdown, compaction and deallocation are preserved. | dk64_deferred_free_checks compiles the actual generated routine: it failed before the patch and passes with a mismatching uncached word, two countdowns, compaction and an empty queue. | Keep this generated-code regression when changing allocator or instruction hooks. |
| [func_global_asm_80600674](../patches/patches_main.c#L97) | Minimum lag, cooperative waiting, DKTV pacing and frontend volume updates | partial: Native timing.cpp carries lag and scheduling corrections. Desktop AlterVolumes configuration synchronization is intentionally omitted because the Vita retains the original in-game volume controls. | dk64_timing_checks covers lag, waits, story intervals and DKTV; host/native intro validation. Original ROM BGM/SFX defaults are both 40. The desktop API maps its 100-percent default to 40, then AlterVolumes forces those configured values into the game. The original 8002CFA4 sound-menu handler passes 32 clamp/input/mixer-call checks plus default reset: SFX updates four channel levels and BGM refreshes sequence channels 0/2. A compiled-AlterVolumes positive control overwrites lower in-game values with the frontend defaults, confirming why that bridge should not be imported alone. Mixer callbacks are recorded, not executed. | Validate native sound-menu controls and audio continuity while playing; the volume-control path is accounted for, but these setter checks do not establish audio quality or complete timing parity. |
| [func_dk64_boot_800009D0](../patches/patches_main.c#L182) | Cached/uncached boot-memory access | equivalent: The runtime mirrors KSEG1 to the same guest RAM, so the original uncached read sees the data without rewriting this function. | RECOMP_MIRROR_KSEG1 in the runtime/recompiler; address-alias tests and successful native boot. | Retain alias tests when changing guest addressing. |
| [func_global_asm_805FE544](../patches/patches_main.c#L203) | Extra display-list allocation for injected desktop commands | retained_original: Vita emits the original display lists and does not insert the matrix/viewport interpolation commands that require the larger desktop arenas. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_805FB944](../patches/patches_main.c#L270) | Force 240p and adjust viewport/overscan modes | partial: Vita carries the Nintendo-logo 240p instruction patch. It deliberately retains original 4:3 overscan/endpoints and four letterbox-bound writes. Upstream moves those writes to its per-frame 805FBFF4 loop, using recomp_get_cutscene_bordering (0 or 40); both drawing and the original closing-viewport consumer still read those fields. Vita keeps their original map-load initialization because it does not import that desktop per-frame update. | Compared the actual Vita-generated setup with compiled upstream in 73728 paired cases: both logo/ordinary map branches, six cutscene values, all 256 combinations of the eight mode-flag bits, all three TV types, four startup-flag combinations and two argument bytes. Helper-call sequences/arguments, VI mode-table pointers, blanking/features, startup flags, caller stack/return address and saved integer registers 16 through 23 match. Only the bounds and four omitted letterbox writes differ. Restoring the actual original ROM instruction 0x240e0002 at 0x805FB980 gives the unpatched 640-pixel logo width; the Vita patch gives 320. After switching from full-screen to ordinary mode, 512 runs of the actual original 80703850 closing-viewport consumer expose 243 differences if only upstream setup is imported, even after normalizing overscan: at amount 0, retained bounds are [10,40,309,199], while missing letterbox updates leave [10,30,309,209]. The real generated 805FCA64 selects the letterbox path from game flags; all 256 non-letterboxed cases match. The comparison passes AddressSanitizer; OS/layout helpers are modeled, so this does not establish final multiplayer/minigame pixels. Evidence: build/upstream-viewport-review/validation.json. | Validate actual multiplayer/minigame viewport output on native Vita builds and physical hardware. Keep the original letterbox initialization and its drawing/closing consumers together; this routine's remaining source differences are accounted for. |
| [func_global_asm_805FBC5C](../patches/patches_main.c#L347) | Initialize startup queues/timer and move the logo sound to presentation | partial: The early playSound call is removed and invoked once at the visible-logo handoff in 805FBFF4. Vita retains the original 0.3-second initialization timer and 192-entry decompression queue together with its original job/DMA arrays; the desktop 0.1-second timer and enlarged queue are not required by this CPU-logo path. | Compared all 106 generated instructions with the upstream initializer: queue capacities are 50, 192, 16 and 2; countdown 0x00D693A4 is 0.3 seconds at 46.875 MHz. The one-instruction sound-call removal regenerates successfully, and before/after runtime traces plus the boot helper regression validate the new handoff. Existing generated queue-boundary/ownership tests cover the retained 192-entry arrangement. | Confirm startup sound and timing on physical Vita; retain queue/array capacity tests when changing the memory budget. |
| [func_global_asm_80658E8C](../patches/patches_main.c#L411) | Relax projected X-bound culling | retained_original: Upstream removes the X-bound rejection while retaining the Y bounds. Vita keeps the original 4:3 projection and its matching original visibility test. | Patch/reference or generated-source inspection plus the fixed 4:3 presentation in rt64_fast_vitagl.cpp. No dedicated later-map visibility test is claimed. | Validate representative camera edges and model-distance changes; reconsider if widescreen or interpolation is added. |
| [func_global_asm_80714A68](../patches/patches_main.c#L464) | Remove original overscan borders/scissor limits | retained_original: The current Vita presentation uses the original 4:3 game viewport/scissor behavior. These desktop no-overscan changes are not selected. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_807132DC](../patches/patches_main.c#L472) | Remove original overscan borders/scissor limits | retained_original: The current Vita presentation uses the original 4:3 game viewport/scissor behavior. These desktop no-overscan changes are not selected. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80714A9C](../patches/patches_main.c#L485) | Remove original overscan borders/scissor limits | retained_original: The current Vita presentation uses the original 4:3 game viewport/scissor behavior. These desktop no-overscan changes are not selected. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80655DD0](../patches/patches_main.c#L625) | Use viewport scissors during desktop chunk drawing | retained_original: Upstream determineChunkFix uses the full screen width and the player's vertical bounds in single-player, or the player's full viewport in multiplayer. The original uses each chunk's clipping rectangle. Both supply chunk bounds to CPU culling and restore viewport bounds afterward. Vita retains the original scissors and visibility pipeline at 4:3. | Compared all 882 original instructions with the compiled upstream routine/helper. Across 96 paired cases varying loaded chunks, render passes, global draw flags, player count and alternate-list selection, ordinary display-list words and draw-helper arguments match after the known per-chunk scissor differences are accounted for. Original and desktop scissor values and restored CPU bounds were checked explicitly. Geometry drawing helpers are modeled. | Validate narrow portals, multiplayer views and screen squish effects on Vita; change traversal, culling and scissor policy together if widening the projection. |
| [func_global_asm_80701CA0](../patches/patches_main.c#L770) | Replace the underwater-tint vertex quad with a primitive rectangle | equivalent: Vita emits the upstream primitive rectangle using each player's viewport. It skips the original 64-byte vertex allocation/construction and two-triangle submission while retaining eligibility, map exclusions, opacity smoothing and camera-state updates. The original projected quad tints an extra row outside a contracted viewport. | The generated-entry Fast F3DEX2/GLES regression fails before the hooks and passes afterward across normal, contracted and two-player bounds with batching off/on. It checks every pixel, opacity state, caller registers and absence of unused allocation/free calls. An independent compiled-upstream comparison matches complete emitted command streams, selected mutable state and flag calls in 576 cases covering nine maps, water-state variants and zero/one/two/four players. Graphics fixtures use controlled state; the original contracted-view replay has 160 incorrectly tinted pixels. | Validate underwater entry/exit, Galleon exclusions and split-screen appearance in native gameplay and on physical Vita; command and controlled pixel checks do not establish all scene interactions. |
| [func_global_asm_8068D264](../patches/patches_main.c#L872) | Replace the sandstorm textured quad with a screen-space textured rectangle | equivalent: Vita carries the upstream viewport texture rectangle, screen-space texel mapping and negative-scroll wrap that adds 255 while preserving fractional overshoot. The original texture acquisition/release, one-cycle MODULATEIA_PRIM mode, color/opacity and already-fractional half-lag calculation remain. | The generated-entry Fast F3DEX2/GLES regression fails before the hooks and passes afterward. Its 64 cases compare every pixel against a patterned IA8 texture, viewport coverage, phase updates and texture ownership across both map-color paths, four lags, wrap boundaries and batching modes. Complete commands, scroll state and texture callback arguments also match compiled upstream in 360 cases including lag 64, negative phases, three cooldowns and one-pixel bounds. The fixture models texture retrieval/release and supplies controlled graphics state. | Validate the sandstorm in native gameplay and on physical Vita, including camera/viewport changes and scene blending; the focused synthetic-texture checks do not establish every visual interaction. |
| [func_global_asm_80703AB0](../patches/patches_main.c#L931) | Draw a uniform cutscene fade using the supplied alpha | equivalent: The native helper emits the upstream classic F3DEX2 primitive-rectangle sequence, including cloud-surface blending and the existing scissor. It replaces the original quad whose alternating corners use alpha and doubled alpha, and skips the draw when alpha is zero. | Native helper output matches the compiled upstream replacement in 2048 command-stream cases covering every alpha, four bounds sets and cached/uncached list aliases. The generated-entry GLES regression failed before the hook and now verifies uniform pixels, inherited clipping and caller state across both banks and batching modes. This exercises the real Fast interpreter/renderer with controlled initial graphics state. | Validate the affected cutscene transition in native gameplay and on physical Vita; command/pixel tests do not establish every caller scenario. |
| [func_global_asm_807035C4](../patches/patches_main.c#L948) | Keep DK Rap static and stripes inside the current viewport | equivalent: Three hooks replace the original fixed 10,10..309,229 scissor and stripe x constants with current viewport bounds. The original primitive colors, noise mode, ten-stripe loop and game RNG remain. This also fixes contracted 4:3 views, not only expanded desktop views. | Original and compiled upstream initially agree at the normal overscan viewport, but the original spills noise outside contracted views. The real generated-entry/F3DEX2/GLES regression fails before the bounds hooks and passes afterward. Patched commands and final RNG state match compiled upstream in 56 viewport/seed cases, including full, split, narrow and one-pixel views. rand_recomp is the generated game wrapper that masks 806119A0, not host libc randomness. | Validate the static sequence during native Rap/camera transitions and on physical Vita; the targeted pixel fixture uses controlled graphics state. |
| [func_global_asm_806522CC](../patches/patches_main.c#L973) | Disable chunk rectangle culling | retained_original: The replacement returns true unconditionally. Vita keeps the original chunk bounds because it has not widened the game projection. | Patch/reference or generated-source inspection plus the fixed 4:3 presentation in rt64_fast_vitagl.cpp. No dedicated later-map visibility test is claimed. | Validate camera edges and portals; keep the original clipper, chunk bounds, traversal and distant-object gate together unless widening the projection. |
| [func_global_asm_8062DBDC](../patches/patches_main.c#L980) | Bypass map-frustum rejection | retained_original: The replacement changes the rejection returns to true. The native path keeps original culling with the unchanged 4:3 camera. | Patch/reference or generated-source inspection plus the fixed 4:3 presentation in rt64_fast_vitagl.cpp. No dedicated later-map visibility test is claimed. | Validate representative camera edges and model-distance changes; reconsider if widescreen or interpolation is added. |
| [func_global_asm_80601EE4](../patches/patches_main.c#L1033) | Read queued audio length instead of the AI hardware register | equivalent: The inline AI_LEN load uses the native runtime query. The following original instruction reloads v0, preserving its live value. | us.vita.toml at 0x80601F0C, generated instruction sequence, and native audio queue traces. | Keep audio delivery checks; the wakeup race is handled separately in func_global_asm_8060F254. |
| [func_arcade_800259D0](../patches/patches_main.c#L1113) | Force point filtering for the arcade image | retained_original: The reduced renderer supports the original N64 filter mode; this desktop appearance preference is not selected. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Validate arcade rendering and input; source capability is not a completed arcade playthrough. |
| [func_global_asm_80704960](../patches/patches_main.c#L1157) | Remove original overscan borders/scissor limits | retained_original: The current Vita presentation uses the original 4:3 game viewport/scissor behavior. These desktop no-overscan changes are not selected. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80658134](../patches/patches_main.c#L1198) | Remove selected viewport clipping passes | retained_original: Upstream removes X viewport clipping passes. The current renderer does not expand the game projection, so the original clipper remains paired with it. | Patch/reference or generated-source inspection plus the fixed 4:3 presentation in rt64_fast_vitagl.cpp. No dedicated later-map visibility test is claimed. | Validate camera edges and portals; keep the original clipper, chunk bounds, traversal and distant-object gate together unless widening the projection. |
| [func_global_asm_80656F14](../patches/patches_main.c#L1268) | Broaden portal traversal and remove the original distant-screen visibility path | retained_original: Desktop traversal carries parent bounds instead of intersecting projected portal rectangles, drops a linked-geometry intersection check, and removes the separate billboard-visibility marking path. Its distant-screen replacement then ignores that visibility flag. Vita retains the original traversal, rectangle clipping and billboard path as one coherent implementation. | Original caller 80653B80 resets visibility, marks the current chunk, traverses portals, then updates distant screens. Compared original 361-instruction traversal and compiled upstream in 192 paired constructed cases with nested windows inside the global viewport. Real generated rectangle-union/intersection and descriptor-lookup helpers verify visibility flags, recursion, adjoining-chunk loading and linked geometry. An incomplete pair (desktop traversal plus original distant processing) suppresses the billboard in eight cases where the original pair enables it. Geometric predicates are modeled. | Validate real doorway traversal and distant objects; preserve the separate billboard path while its consumer remains portal-gated. |
| [func_global_asm_8066308C](../patches/patches_main.c#L1404) | Update distant-screen records independently of portal visibility | retained_original: The desktop patch makes distance, opacity and angle updates unconditional. Vita retains the original gate indexed by the descriptor's portal entry; its original traversal includes the special path that marks visible distant screens even when the adjoining chunk is not loaded. | Compared all 262 original instructions with the upstream body. Paired traversal/consumer cases confirm enabled/disabled behavior and preservation of cached parameters while hidden. Another 72 enabled numeric pairs match full records across near/intermediate/far opacity cases, positive/negative elevation and heading-wrap samples. Original 806634A4 consumes only records with the active byte set. Geometric and heading/elevation helpers are modeled; this is not a real scene render. | Validate distant billboards through portals and behind scene geometry on Vita; pair any gate removal with the traversal policy change. |
| [func_global_asm_80704484](../patches/patches_main.c#L1453) | Cover stopped mid-map transitions and adjust desktop transition bounds | partial: A native entry hook emits the upstream opaque-black cover when speed is zero and progress exceeds 28. It reserves its own guest argument area and returns the advanced display-list pointer. Other original transition paths remain; desktop no-overscan changes are not imported. | The upstream stopped-transition branch and generated entry hook were checked. dk64_gameplay_fix_checks covers the threshold, active-animation rejection, black/alpha arguments, display-list result and caller-stack preservation. | Validate map transitions in gameplay and review the remaining desktop bounds changes separately. |
| [createLight](../patches/patches_main.c#L1590) | Expand the light array from 16 to 48 entries | retained_original: The original code already rejects a 17th light. Vita retains that original array and matching bound rather than importing the expanded desktop storage. | Original C and generated instructions at 0x8065A714/0x8065A71C both compare the count with 0x10; the patch uses LIGHT_LIMIT 0x30 with its own array. | Keep the original producer, table and consumer limits together; validate dense light/shadow scenes on physical Vita before changing capacity. |
| [func_global_asm_8065A884](../patches/patches_main.c#L1647) | Process actor recoloring and shadows using the desktop expanded light array | retained_original: The original already guards zero direction/distance and shadow denominators. The replacement switches from the original 16-entry light table to the desktop 48-entry table; the per-pass 12-light cap, player-count restriction, actor exclusions and shadow fallback remain. Vita keeps its original producer/table/consumers together. | Compared all 1159 original instructions and the upstream body, then executed both generated routines against their respective constants/light tables. All 384 paired cases match helper calls/values exactly, including point/directional lights, zero-length directions, coincident positions, horizontal/vertical shadow cases, actor exclusion, three-player restriction, selective lights and counts 0/1/12/16. Light applicability, recoloring, heading and shadow draw helpers are modeled; no rendered lighting claim. | Validate representative light/shadow scenes on physical Vita; change the producer and both table consumers together if expanding the light budget. |
| [func_global_asm_8065BAA0](../patches/patches_main.c#L1898) | Select and pack up to 12 lights from the desktop expanded light array | retained_original: The original already substitutes 0.1 for zero direction length and zero radius before computing light coefficients. Upstream redirects its source to the expanded light table. Vita retains the original 16-entry table with the same 12-record output limit and original coefficient/flag construction. | Original guards verified at 0x8065BD08..0x8065BD24 and 0x8065BD78..0x8065BD90. In 384 paired original/compiled-upstream cases, packed records, mutated radii, selected count, flag and helper values match exactly. Used coefficients/colors remain finite for the tested degenerate inputs; output stays within 12 records. Applicability/finalization helpers are modeled. | Retain these guards and the 12-record output bound if changing input capacity; physical scene shading remains separate validation. |

### patches/patches_matrix.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_80614B34](../patches/patches_matrix.c#L169) | Tag model/sprite/text transforms for interpolation | retained_original: The current backend consumes original N64 matrices and renders at the game rate. It does not require inserted matrix groups or their larger command allocations. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80636FFC](../patches/patches_matrix.c#L258) | Apply desktop prop draw-distance floors and tag prop-model matrices | retained_original: Retain the original prop visibility, opacity, lighting and model matrices at the native game rate. Upstream adds configurable minimum draw distances plus identity-matrix pushes and interpolation groups. Its prop filter excludes melon crates from the added distance floor; these desktop features are not enabled by the Vita integration. | Paired the original 732-instruction routine with the compiled upstream replacement. With the desktop distance floor zero and only interpolation groups/balanced identity pushes removed from command comparison, 672 paired cases match commands, model/matrix/script state and helper arguments exactly. Cases vary seven prop types, distance boundaries, both frame banks, player index, script/lighting/opacity branches and animation. The fixture uses real generated gu matrix functions and models gameplay helpers. A positive distance-floor control changes a regular prop but preserves the excluded melon crate. | Validate prop visibility and fades in later maps on physical Vita; revisit draw-distance consumers and matrix-group storage together if desktop distance or interpolation features are added. |
| [func_global_asm_80714B84](../patches/patches_matrix.c#L441) | Assign interpolation identities to sprites | retained_original: Vita draws original N64 frames without desktop sprite interpolation groups. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80714C08](../patches/patches_matrix.c#L452) | Assign interpolation identities to sprites | retained_original: Vita draws original N64 frames without desktop sprite interpolation groups. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [drawSpriteAtPosition](../patches/patches_matrix.c#L466) | Assign interpolation identities to sprites | retained_original: Vita draws original N64 frames without desktop sprite interpolation groups. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806A370C](../patches/patches_matrix.c#L496) | Textbox glyph matrix/interpolation identities | retained_original: The replacement adds textbox interpolation fields and matrix-group commands. Vita keeps the original text transforms and allocation layout; the reduced backend does not implement those extended commands. | Full patch body and rt64_fast_unsupported.cpp; no textbox-specific runtime proof. | Reassess when enabling the corresponding frontend, larger guest allocations or extended-GBI feature; retain explicit validation limits. |
| [func_global_asm_806A4284](../patches/patches_matrix.c#L598) | Increase text-bubble display-list storage for desktop matrix tagging | retained_original: Retain the original 0x4000-byte child-list allocation with its deferred release and existing text-bubble renderer. Upstream requests 0x5000 bytes to accommodate matrix tags emitted by its changed glyph renderer; those tags are not inserted by the Vita path. | The original 438-instruction routine and compiled upstream replacement match display-list words, matrices, animation state and helper arguments in 576 paired cases; only the allocation request differs. Cases cover empty/single/multiple linked nodes, iteration limits and early stop, both banks, two player views, custom scissor bounds, scale and frame-wrap boundaries. Actual generated matrix routines run; background and glyph helpers are modeled. | Validate full text bubbles and glyph capacity in native/physical scenes. Reassess allocation size with the glyph renderer if adding matrix tags. |
| [func_global_asm_80637B6C](../patches/patches_matrix.c#L721) | Apply a desktop prop-sprite draw-distance floor and tag sprite matrices | retained_original: Retain original sprite distance culling/fading, frame-bank matrices and texture modes. Upstream adds its configurable minimum distance plus an identity-matrix/interpolation group around the sprite-layer loop. The current Vita renderer consumes original game frames and does not use those groups. | Paired the original 1492-instruction routine with compiled upstream. After disabling the desktop distance floor and removing only interpolation groups/balanced identity wrappers, 384 paired cases match commands, stored matrices, texture-cache state and helper arguments exactly. They cover four texture sizes, CI palettes, dual-texture blending, both frame banks, two player indices, culling/lighting flags and distance boundaries. Real generated gu matrix functions are used; projection, sprite decoding, lighting and visibility predicates are modeled. Raising the desktop floor changes all four distant-sprite positive controls. | Validate actual prop sprites, palette transitions and distance fades in later maps on physical Vita; the modeled helpers and command comparisons do not prove final scene pixels. |
| [func_global_asm_8069F904](../patches/patches_matrix.c#L966) | Tag model/sprite/text transforms for interpolation | retained_original: The current backend consumes original N64 matrices and renders at the game rate. It does not require inserted matrix groups or their larger command allocations. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8069FA40](../patches/patches_matrix.c#L994) | Tag model/sprite/text transforms for interpolation | retained_original: The current backend consumes original N64 matrices and renders at the game rate. It does not require inserted matrix groups or their larger command allocations. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8065FD88](../patches/patches_matrix.c#L1139) | Tag fluid matrices and vertices for desktop interpolation | retained_original: The original fluid list selection and one-time display-list initialization remain. Upstream surrounds each selected fluid with an identity-matrix push/pop and a vertex-interpolation group, assigning a per-pass group ID. Vita retains the original rendering path without interpolated intermediate frames. | Paired the original 76-instruction routine with compiled upstream: 256 paired cases match normalized commands, helper arguments and final fluid flags for empty/single/multiple lists, both banks, pass filters, all low three flag bits and repeat draws. Normalization removes only interpolation groups and balanced identity wrappers. Scissor and mesh-initialization helpers are modeled. The original 300-instruction 80660070 mesh builder emits vertex/triangle and RDP commands, with no matrix push/load/pop commands. | Validate water/lava geometry and scissor boundaries during physical gameplay; these list-selection/initialization comparisons do not render the fluid meshes. |
| [func_global_asm_806187E8](../patches/patches_matrix.c#L1187) | Suppress hand interpolation across animation changes | retained_original: Vita has no interpolation history for these hand transforms, so the extra lockdown state is unnecessary. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80618820](../patches/patches_matrix.c#L1195) | Suppress hand interpolation across animation changes | retained_original: Vita has no interpolation history for these hand transforms, so the extra lockdown state is unnecessary. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068A764](../patches/patches_matrix.c#L1203) | Suppress hand interpolation across animation changes | retained_original: Vita has no interpolation history for these hand transforms, so the extra lockdown state is unnecessary. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068A784](../patches/patches_matrix.c#L1208) | Suppress hand interpolation across animation changes | retained_original: Vita has no interpolation history for these hand transforms, so the extra lockdown state is unnecessary. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80701098](../patches/patches_matrix.c#L1238) | Tag the twelve lens-flare image transforms for interpolation | retained_original: Retain original flare vector math, intensity/color calculations, image arguments and bright-screen tint. Upstream adds a separate identity-matrix/interpolation scope for each of the twelve image draws. Those scopes serve the desktop interpolation path; Vita retains the original displayImage matrix behavior. | Paired the original 766-instruction routine with compiled upstream. Ninety-six paired cases varying light direction, opacity, color and frame bank match emitted commands and helper arguments exactly after removing only interpolation groups/balanced identity wrappers. Vector add/subtract/scale/normalize/dot helpers execute their real generated code. Projection, displayImage and full-screen tint are modeled. This comparison does not assert identical post-call modelview state: upstream scopes displayImage matrix loads, while the original keeps its established caller behavior. | Validate lens-flare appearance and following overlays in native gameplay and on physical Vita; evaluate displayImage state and matrix-group allocation together if importing interpolation. |
| [func_global_asm_807007B8](../patches/patches_matrix.c#L1347) | Assign stable interpolation identities to visible lens-flare groups | retained_original: The original driver already performs flare render-state setup, selects the opposite frame-bank visibility counters, decrements nonzero counters, invokes the flare renderer and restores dithering. Upstream adds the per-flare base transform ID consumed by its new image interpolation groups. | Paired the original 143-instruction routine with compiled upstream. In 384 paired zero/one/two/three-flare cases, both banks and varied light directions/colors/visibility counts, normalized commands, projection/image/tint arguments and all six visibility-counter bytes match. Each driver calls its corresponding actual 80701098 implementation; only interpolation groups and balanced identity wrappers are removed from command comparison. Projection and drawing helpers remain modeled, so this is not a pixel or complete downstream-state test. | Validate flare visibility and transitions to subsequent overlays on physical Vita; revisit the driver ID assignment together with the twelve-image interpolation groups. |
| [func_critter_80028A9C](../patches/patches_matrix.c#L1398) | Tag model/sprite/text transforms for interpolation | retained_original: The current backend consumes original N64 matrices and renders at the game rate. It does not require inserted matrix groups or their larger command allocations. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_critter_80028EE8](../patches/patches_matrix.c#L1458) | Assign stable interpolation identities to DK Rap lyric lines | retained_original: The added line_index reset/increment feeds only the desktop lyric matrix tags. Vita keeps the original lyric allocation, positioning and string copy; its renderer does not interpolate tagged matrices. | Full patch/reference comparison checked against generated MIPS: 0x80028F14 already multiplies the string index by 32, 0x80028FE0 already selects offset 0x820, and 0x80028F58/0x80028F60 retain the original frees. Native Vulkan runs render the DK Rap lyrics. The apparent string-layout changes in the C reference are type reconstruction differences. | Retain original lyric lifetime and layout; stable line identities are required only if matrix interpolation is introduced. |

### patches/patches_ui.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_80715E94](../patches/patches_ui.c#L77) | Tag sprite transforms and support desktop HUD alignment flags | retained_original: Retain original sprite visibility, child display-list construction, lighting and texture-cache behavior for alignment values 0/1. Original 8071495C sets the on-screen flag to 1, 80714A9C resets it to 0, and the constructor copies it to sprite offset 0x36F. Upstream adds sprite IDs, matrix-interpolation wrappers and extended alignment/overscan flags. The separate desktop culling bypass is not imported. | The original 1055-instruction routine and compiled upstream replacement match normalized parent commands, complete child lists, sprite/cache state and helper arguments across 768 drawing cases and 432 visibility cases. Cases cover all four texture sizes, zero/one/four layers, both frame banks, first/fourth player and list slots, cache hits, lighting, chunk selection and visibility flags. Only parent interpolation tags and balanced identity push/pop pairs are removed; all other low-8-MiB memory outside the reserved parent-list buffer is compared. The actual generated display setup and upstream matrix-group helpers run; a common modeled culling predicate and lighting callback are used. Extra desktop alignment values are excluded by the retained original producer policy. | Validate world/HUD sprite transitions and final pixels on physical Vita. As with lens flares, the additional identity scope can affect post-call modelview state; normalized command equality does not prove complete graphics-state equivalence. Revisit producers, culling, allocation and consumers together if enabling extended alignment or interpolation. |
| [func_global_asm_806F9D8C](../patches/patches_ui.c#L254) | Anchor HUD numbers and replace their classic viewport scissor with desktop alignment scopes | retained_original: Retain original HUD number formatting, fade/translation matrices, right-side string centering and viewport clipping. Upstream adds left/right origin alignment and extended viewport/scissor scopes, moves the viewport query earlier and replaces the original classic scissor. Those desktop scopes are not used by the 4:3 Vita path. | The original 262-instruction routine and compiled upstream replacement match matrices, alpha, formatted strings, text arguments and remaining classic commands in 2700 paired cases after explicitly accounting for their scissor/alignment commands and the moved viewport query. Cases cover normal/infinite/unavailable numbers, crystal conversion, slide phases, both frame banks and left/center/right positions. Generated matrix, game trig and rounding routines run; font and viewport helpers are modeled. The comparison was rerun with the actual game math and retained the same result. For the 1620 centered cases, compiled upstream emits one scissor push and one viewport push without matching pops in this routine; original emits neither. Actual HUD initializers include centered X values 82, 122 and 194. | Validate visible numeric HUD transitions on physical Vita; importing extended alignment requires checking scope balance and the complete sprite/viewport pipeline. Command-scope evidence does not prove a desktop runtime failure. |
| [func_menu_80030894](../patches/patches_ui.c#L325) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806F9744](../patches/patches_ui.c#L424) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806FF144](../patches/patches_ui.c#L537) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8071338C](../patches/patches_ui.c#L556) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_807138CC](../patches/patches_ui.c#L582) | Expand the DKTV overlay viewport and shift its title for desktop overscan handling | retained_original: Retain original DKTV viewport bounds 10,10..309,229 and original title placement at 4:3. Upstream changes the viewport to 0,0..320,240 and incorporates its shifted title helper. Allocation/deferred release, layer order, frame-counter advance and fade calculations remain equivalent in the controlled comparison. | The original 119-instruction driver and compiled upstream replacement match commands, counter state and helper arguments across 240 consecutive starting counter values after checking exactly the viewport-bound change and title X shift from 280 to 200. The original title helper executes its generated code; compiled upstream inlines the revised title body. Allocator, viewport construction and drawing callbacks are modeled. The viewport and title decisions remain paired with the existing 8071338C/806FF144 audit entries. | Inspect the full DKTV overlay in physical gameplay, including borders and fade timing; the callback comparison is not a rendered-overlay test. |
| [func_global_asm_8068D8C8](../patches/patches_ui.c#L607) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806F9B64](../patches/patches_ui.c#L626) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_bonus_8002733C](../patches/patches_ui.c#L651) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8070068C](../patches/patches_ui.c#L674) | Preserve desktop HUD alignment when setting the sprite viewport | retained_original: The added condition skips the original scissor when desktop sprite alignment is at least two. Vita retains the original sprite/scissor path and does not import the gEX alignment helpers. | Full reference-C diff changes only the scissor guard. alignHUD/popHUD and setSpriteAlignment implement the paired desktop scissor/viewport state; no Vita-specific scissor correction is established by this replacement. | Revisit with widescreen or extended HUD alignment; check native HUD clipping in the affected menus/minigames. |
| [func_global_asm_806A2B90](../patches/patches_ui.c#L691) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068DC54](../patches/patches_ui.c#L761) | Expand rolling-counter horizontal clipping for desktop HUD alignment | retained_original: Retain the original digit-window scissors and 12-step rolling-count behavior. The first window spans X..X+52; the hundreds window starts at max(X-20,0). Both retain the 27-pixel vertical band and restore the viewport afterward. Upstream widens these windows to the character viewport and adds optional extended HUD alignment scopes. | The original 520-instruction routine and compiled upstream replacement match counter/phase updates, trig input, emitted strings/text arguments and non-scissor commands in 2496 paired cases. Cases cover increment/decrement/equal targets, all phases 0..12, 9/10, 99/100 and 199/200 boundaries, three X positions and two Y positions. Every original and upstream scissor is checked against its respective bounds; vertical clipping and final viewport restoration agree. The generated game sine routine runs; formatting, projection setup and text rendering are modeled, with original on-screen alignment value 1. The comparison was rerun after replacing the earlier trig model, with the same result. | Validate actual rolling glyphs in minigames on physical Vita; change digit scissors together with alignment if adding desktop anchored HUD behavior. |
| [func_bonus_80024000](../patches/patches_ui.c#L877) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_bonus_800252A0](../patches/patches_ui.c#L901) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_bonus_80026940](../patches/patches_ui.c#L925) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_bonus_80029B9C](../patches/patches_ui.c#L1010) | Apply desktop left alignment to Klamour, Peril Path Panic and Big Bug Bash counters | retained_original: Retain original counter/header selection and game-state field ownership. The upstream wrapper adds extended HUD scopes and sets the sprite alignment byte to ALIGN_LEFT (3) around header/counter calls, resetting it to zero afterward. Vita keeps its original alignment state and classic counter renderer. | The original 174-instruction wrapper and compiled upstream replacement match classic commands, helper arguments and modeled state changes in 288 paired cases across actor types 124/125/126 plus an unrelated type, six control states, initial alignment 0/1, header-enable flags and scores near digit boundaries. Tests verify exact selected counter/target/phase addresses, balanced added wrapper scopes, alignment 3 at upstream drawing callbacks and its final reset. Header/counter rendering and the optional bonus helper are modeled; this is separate from the Klamour timer fix. Native ARM Klamour practice runs now render the original header/counter, register real hits, clear all four target requirements and return to Snide HQ under Linux Vita3K/Vulkan. A native Normal screenshot shows HIT 14 after the first success, and the logs track all counts down to zero. This does not validate Peril Path Panic or Big Bug Bash graphics; see docs/VITA_KLAMOUR_VALIDATION.md. | Validate counters and headers during each affected minigame on physical Vita; retain the original wrapper and rolling-counter alignment policy together. |
| [func_bonus_8002CC08](../patches/patches_ui.c#L1084) | Align the Searchlight Seek header and counter for desktop HUD placement | retained_original: Vita retains the original projected target markers and counter coordinates. The replacement adds paired HUD/sprite alignment calls; the reordered temporaries and actor-data union access do not alter their storage. | Full reference-C comparison plus Actor union layouts, u32 object_timer declarations and the s16 displayImage coordinate signature. Marker frame arithmetic, render modes and control-state cases are retained. No Searchlight Seek playthrough has been performed. | Validate the original 4:3 counter and projected markers during a Searchlight Seek round; port alignment only with the corresponding renderer feature. |
| [func_bonus_8002D010](../patches/patches_ui.c#L1130) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_boss_800286B8](../patches/patches_ui.c#L1187) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806FF75C](../patches/patches_ui.c#L1209) | Widen sniper-scope outer masks and override player clipping for the desktop view | retained_original: Retain the original per-player viewport/scissor set by 806FEDB0 and the original scope positions at 320x240. Upstream adds a full-screen scissor and moves four outer-mask X positions according to desktop UI width. The targeting query, upgrade guard, inner masks and reticle color/position do not need replacement at native width. | The original 244-instruction routine and compiled upstream replacement match remaining commands and callback arguments in 56 paired cases after checking/removing exactly the added 0,0..320,240 scissor. Cases cover both frame banks, one/two/four players and all upgrade/hit combinations. The real generated 806FEDB0 supplies per-player clipping and viewport addresses; image drawing and target queries are modeled. An expanded-width positive control moves exactly four outer images by plus/minus 53 pixels at width 426 and leaves the inner masks/reticle unchanged. | Check visible scope masks and clipping in single-player and split-screen on physical Vita. The controlled command/callback comparison does not render the mask textures. |
| [func_race_8002BDDC](../patches/patches_ui.c#L1283) | Draw Tiny Car Race map dots with integer-position texture rectangles | retained_original: Retain the original map-coordinate calculations and projected image-quad rendering at 4:3. Upstream converts the same 16x16 dot into a four-pixel texture rectangle, truncating its top-left position to integers. The original projection preserves the fractional position and its existing texture sampling. | Inspected the original 67-instruction routine and compiled upstream replacement, then executed their complete image paths through Fast F3DEX2/GLES with original generated trig, matrix and vertex helpers. Eighteen paired dot replays cover solid/patterned/actual US-ROM texture 0x4A, point/bilinear filtering and three fractional positions. Solid dots share their four-pixel footprint at integral centers; a 0.75-pixel position moves the original footprint by one pixel while the rectangle truncates it. Actual texture sampling differs, so this is an intentional original-renderer decision rather than a pixel-equivalence claim. | Validate moving map dots in the Tiny Car Race on physical Vita. Import rectangle placement together with the desktop HUD alignment policy if that feature is added. |
| [func_race_8002CBEC](../patches/patches_ui.c#L1303) | Align the Tiny Car Race minimap and replace its image quads with texture rectangles | retained_original: Retain the original 4:3 map tiles, participant-dot dispatch and image-resource lifetime. Upstream adds left HUD alignment and draws the four map tiles/dots with its integer-coordinate rectangle helper. The original projection and image path render the connected map without needing those desktop alignment commands. | Paired the original 257-instruction routine with compiled upstream. In 288 map-selection cases, texture IDs, primitive colors, continuation calls and persistent input state match across map visibility, zero/one/two participants, participant types, null entries, extra markers and both banks. Original image allocations have matching deferred-release calls; upstream uses no image allocations. Six full-image GLES pairs execute real generated image/matrix/trig/vertex routines with solid, patterned and actual table-14 textures 0x46..0x49. Map/background bounds agree at 4:3; actual map sampling differs by more than one 8-bit RGB unit at 196 pixels with point filtering and 295 with bilinear filtering, while both render a connected map. Desktop alignment commands are excluded and a common original scissor is supplied. | Check the complete minimap and racers in native/physical gameplay, including visibility transitions. The replay uses controlled graphics state and does not establish every caller or extended-aspect behavior. |
| [func_race_8002C14C](../patches/patches_ui.c#L1399) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_race_8002C76C](../patches/patches_ui.c#L1428) | Align race position text and use a stack buffer for the lap string | retained_original: Retain original lap clamping, position/status text selection, message dispatch and string-sized allocation with deferred release. Upstream adds balanced left-alignment scopes and replaces the allocated lap string with a 64-byte local buffer. The commented var_t0-and-itself expression has no required behavior to port. | The original 215-instruction routine and compiled upstream replacement match classic commands, text/helper arguments and persistent input state in 1440 paired cases after removing only extended HUD alignment commands. Tests cover visibility flags, lap clamping including zero/overshoot, total laps, rank/final-position selection, finish flags, message states, both banks and even/odd font heights. The actual generated 8002C63C/guScale projection setup runs. Font/string callbacks are modeled; allocation accounting confirms one correctly sized allocation/deferred release when lap text is shown versus upstream stack storage. | Validate visible lap/rank/status text during each race on physical Vita. Keep the original text lifetime unless the drawing consumer and alignment policy are changed together. |
| [func_race_8002C2E8](../patches/patches_ui.c#L1486) | Align missed-gate text and replace gate icons with texture rectangles | retained_original: Retain original missed-gate label/count selection, projected icons and deferred image-resource release at 4:3. Upstream replaces those quads with integer-position 12x12 rectangles and adjusts coordinates for their top-left origin. The original and rectangle paths have different sampling and about a one-pixel horizontal placement difference in the controlled replay. | Compared the original 213-instruction routine with compiled upstream, including the original 8002C63C half-scale projection setup. Twelve paired zero-through-five missed-gate cases with even/odd font heights match label/helper arguments, texture IDs, primitive colors and image counts; original allocation/release pairs are retained. Six complete GLES image pairs use solid, patterned and actual US-ROM textures 0x45/0x4B with point/bilinear filtering. Both draw five gate slots and the selected crosses; pixel sampling differs. Original trig/matrix/vertex and both image implementations execute, while font rendering is modeled and desktop alignment commands are excluded. | Validate label/icon placement and missed-gate transitions in native and physical races. Revisit icon coordinates, sampling and projection together when adding desktop anchored HUD rendering. |
| [func_bonus_8002570C](../patches/patches_ui.c#L1546) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80710CA0](../patches/patches_ui.c#L1811) | Stretch weather particle X positions and add desktop screen alignment | retained_original: Retain original weather coordinates, matrices, suppression checks and particle lifetime at 4:3. Upstream morphWeather scales X by UI width/320 and offsets it for expanded aspect ratio, surrounding drawing with extended HUD alignment. The separate desktop always-visible chunk predicate is not imported; Vita keeps its existing clipping policy. | The original 476-instruction routine and compiled upstream replacement match normalized commands, complete particle records/matrices, free-list updates and helper calls in 192 paired cases at width 320 after removing only extended alignment commands. Cases cover four weather types, 0/1/6/128 particles, both banks, visibility/water/control-state suppression, animation and deletion. Generated matrix and game trig functions run; texture selection, the common culling predicate and update callback are modeled. The comparison was rerun with actual game trig and retained the same result. A width-426 positive control changes only twelve stored matrix X-translation words for six particles, with dispatch and callbacks unchanged. | Validate rain/snow/other weather during native and physical gameplay, including camera changes and underwater transitions. The common modeled predicate does not prove equivalence to the separately disabled desktop culling replacement. |
| [func_global_asm_806F8170](../patches/patches_ui.c#L1935) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806FFB2C](../patches/patches_ui.c#L1954) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806A92B4](../patches/patches_ui.c#L2058) | Align pause text and tag main-menu option transforms for interpolation | retained_original: Retain original pause text, health images, option/confirmation selection, progress totals, shared sprite-position arrays and 8000-byte child-list ownership at 4:3. Upstream adds extended left/right alignment around edge text and matrix groups for menu options and confirmation prompts. | The original 890-instruction renderer and compiled upstream replacement match helper arguments, parent/normalized child lists and persistent guest state in 2400 paired cases after removing only extended alignment and balanced identity/interpolation wrappers. Tests cover all four menu modes, normal/special worlds, option/confirmation states, progress variants, fade limits, visible/hidden page-scroll boundaries and frame-bank/counter values. Generated trig and matrix-group helpers run; font/image drawing and page/progress helpers are modeled. This does not establish identical post-call modelview state for real drawing callbacks. | Validate complete pause-page rendering, confirmation prompts, scrolling and background snapshots on physical Vita; keep alignment, matrix scopes and consumers paired if importing desktop rendering. |
| [func_global_asm_806AC07C](../patches/patches_ui.c#L2356) | Suppress interpolation for large pause-sprite movements | retained_original: Retain original pause-sprite animations, alpha/scale/color updates, wandering targets and removal conditions. Upstream adds ALIGN_NO_INTERP when an X or Y change exceeds 80 coordinate units and clears it otherwise. Vita uses the original game-rate sprite path without interpolation history. | The original 643-instruction routine and compiled upstream replacement match low-8-MiB guest state, including RNG and shared sprite arrays, in 4000 paired mode/page/fade/scroll cases after accounting only for the new interpolation bit. Tests cover animation types 0..15, null metadata, fade clamp boundaries, scroll-removal boundaries, wandering targets and both page banks. Six additional cases check both signs just below, at and above 80. Actual generated angle trig and game RNG routines run; the result is not a rendered-animation test. | Validate visible pause-sprite animation and removal across page changes on physical Vita; import the interpolation bit only with the corresponding sprite renderer support. |
| [func_global_asm_806AB4EC](../patches/patches_ui.c#L2537) | Preserve desktop alignment while initializing pause sprites | retained_original: Retain original sprite lookup, callback registration, metadata sizes, mode remapping, mirroring and availability tint. Upstream makes the on-screen-flag setter conditional so that preselected left/right alignment survives. Original values 0/1 produce the same initialization; Vita retains its original producer/consumer flag policy. | The original 199-instruction constructor and compiled upstream replacement match modeled sprite/metadata state, helper arguments and real RNG state in 512 paired cases covering all mode values, indexed/direct sprite references, page banks, original alignment 0/1, availability and two scales. Callback addresses are normalized to the same update-function identity. Two positive controls confirm that original forces alignment 1 while upstream preserves 3/5, with other state equal. The actual generated RNG runs; sprite creation, allocation and resource lookup are modeled. | Check actual pause sprite resources and animation in native/physical gameplay. Preserve callback, alignment and metadata ownership together if switching to desktop anchored sprites. |
| [func_global_asm_806AA304](../patches/patches_ui.c#L2626) | Add desktop pause-sprite alignment and interpolation reset during page initialization | retained_original: Retain original inventory/progress queries, sprite/text selection, page setup, character-index restoration and sprite metadata behavior. Upstream adds left/right alignment selections and a sprite-interpolation lockdown request with value 2. These additions are paired with desktop renderer features. | The original 1146-instruction initializer and compiled upstream replacement match helper arguments, sprite counts and low-8-MiB state in 384 paired cases after accounting for alignment values 3/5 and the interpolation callback. Both invoke their actual 806AB4EC constructors. Cases cover four menu modes, two characters and banks, normal/special worlds, feature masks and initial/refresh paths. Tests verify restoration of current_character_index. Generated trig, rounding and RNG run; flag/progress queries, text-entry construction, resource lookup and base sprite creation are modeled. | Validate each pause page and its progress-dependent items in native/physical gameplay; these controlled flag-query cases do not establish every save state or resource combination. |
| [func_global_asm_805FD088](../patches/patches_ui.c#L2967) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806C75A4](../patches/patches_ui.c#L3187) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068DBA4](../patches/patches_ui.c#L3307) | Right-align Number Game timer text in the desktop HUD | retained_original: The original timer text rendering remains appropriate for the current 4:3 viewport. The upstream addition brackets the same printStyledText call with extended HUD alignment. | Full reference-C diff preserves the 12-byte text buffer, decimal value, color/combine state and coordinates 0x370/0x50. The new alignHUD/popHUD calls emit gEX viewport/scissor commands; no timer arithmetic is changed. | Validate timer visibility in the Number Game; this source comparison does not claim a native minigame test. |
| [func_global_asm_806FEF7C](../patches/patches_ui.c#L3322) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068DAF4](../patches/patches_ui.c#L3334) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8068E7B4](../patches/patches_ui.c#L3349) | Desktop HUD/menu alignment, bounds or interpolation | retained_original: Vita retains original 4:3 HUD/menu placement and N64 display-list commands. It does not expose the desktop UI-bound setting or emit those alignment/interpolation extensions. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |

### patches/sound_options_patches.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_8068ABE0](../patches/sound_options_patches.c#L47) | Frontend story-skip selection | retained_original: The native port uses the game's existing story/save selection rather than a desktop skip setting. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_807144B8](../patches/sound_options_patches.c#L141) | Restore Helm completion flags on existing saves; desktop story-skip/events | partial: The native helper restores temporary flags 0x4b..0x4f only for an existing save with permanent flag 0x302. Original story selection remains; desktop events/options are omitted. | MIPS ABI assertions; dk64_gameplay_fix_checks covers eligibility, exact flag set, unrelated flags and caller registers; generated hook follows file-status query. | Test loading an affected save on the running game; desktop story-skip/event parity remains separate. |
| [func_global_asm_80621174](../patches/sound_options_patches.c#L182) | Desktop camera-style and analog-camera options | retained_original: Vita currently maps controls to original N64 stick/C-button behavior; it does not expose these frontend camera modes. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806EA200](../patches/sound_options_patches.c#L284) | Desktop camera-style and analog-camera options | retained_original: Vita currently maps controls to original N64 stick/C-button behavior; it does not expose these frontend camera modes. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806EA26C](../patches/sound_options_patches.c#L312) | Desktop camera-style and analog-camera options | retained_original: Vita currently maps controls to original N64 stick/C-button behavior; it does not expose these frontend camera modes. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806EA0A4](../patches/sound_options_patches.c#L345) | Desktop camera-style and analog-camera options | retained_original: Vita currently maps controls to original N64 stick/C-button behavior; it does not expose these frontend camera modes. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_8061D1FC](../patches/sound_options_patches.c#L381) | Frontend analog-camera orbit and zoom | retained_original: The additional orbit/zoom path is gated by analog_cam_enabled/get_analog_allowed and frontend sensitivity. Vita retains N64 C-button camera controls and does not enable that path. | Reviewed the full replacement body, its named feature/event calls, and the current Vita control/build path. No new function-specific gameplay test is claimed. | Revisit when adding the corresponding frontend feature or event API; retain explicit control/save regression coverage. |
| [func_global_asm_80620628](../patches/sound_options_patches.c#L473) | Preserve analog-camera position during camera recovery | retained_original: The added guards avoid recentering an enabled desktop analog camera. That feature is not active in the current Vita control path, so the original recovery behavior is retained. | Reviewed the full replacement body, its named feature/event calls, and the current Vita control/build path. No new function-specific gameplay test is claimed. | Revisit when adding the corresponding frontend feature or event API; retain explicit control/save regression coverage. |
| [func_global_asm_806E6B98](../patches/sound_options_patches.c#L589) | Frontend axis inversion, mouse and gyro options | retained_original: Vita uses its documented original N64 control mapping; desktop inversion/mouse/gyro configuration is not connected. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_806E65BC](../patches/sound_options_patches.c#L637) | Frontend swimming-axis inversion | retained_original: The replacement applies configured axis inversion to the original swimming controls. Vita currently uses its documented original N64 stick mapping. | Reviewed the full replacement body, its named feature/event calls, and the current Vita control/build path. No new function-specific gameplay test is claimed. | Revisit when adding the corresponding frontend feature or event API; retain explicit control/save regression coverage. |
| [func_global_asm_806EA628](../patches/sound_options_patches.c#L679) | Frontend axis inversion, mouse and gyro options | retained_original: Vita uses its documented original N64 control mapping; desktop inversion/mouse/gyro configuration is not connected. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_menu_8002D520](../patches/sound_options_patches.c#L743) | Remove settings moved to the desktop frontend | retained_original: Vita retains the original in-game sound/options menus because those replacement frontend controls are not present. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_menu_8002CFA4](../patches/sound_options_patches.c#L793) | Remove settings moved to the desktop frontend | retained_original: Vita retains the original in-game sound/options menus because those replacement frontend controls are not present. | The actual generated original handler passes 32 SFX/BGM increment/decrement/priority/clamp cases, including values 0, 1, 39 and 40, plus a reset-defaults check. SFX produces four channel updates at volume*625; BGM refreshes sequence channels 0 and 2. ROM defaults and the desktop percentage conversion are verified. Menu navigation, persistence and mixer callbacks are modeled; no audible gameplay test is claimed. | Validate the original sound-menu sliders, mode selection and audible results on physical Vita; keep desktop volume synchronization paired with a fully integrated frontend. |
| [func_menu_8002DBDC](../patches/sound_options_patches.c#L893) | Remove settings moved to the desktop frontend | retained_original: Vita retains the original in-game sound/options menus because those replacement frontend controls are not present. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_menu_8002D8AC](../patches/sound_options_patches.c#L942) | Remove settings moved to the desktop frontend | retained_original: Vita retains the original in-game sound/options menus because those replacement frontend controls are not present. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [isFlagSet](../patches/sound_options_patches.c#L1020) | Frontend multiplayer gating, flag-check events and relocated temporary flags | retained_original: Vita preserves original save-flag semantics and its paired original flag bank. It has no frontend multiplayer-disable toggle or mod flag events; controller presence is reported separately by the native input backend. | Full patch body and platform/vita/main.cpp; only controller 0 is currently exposed. No multiplayer gameplay support is claimed. | Reassess when enabling the corresponding frontend, larger guest allocations or extended-GBI feature; retain explicit validation limits. |
| [setFlag](../patches/sound_options_patches.c#L1046) | Relocated temporary flag bank and mod events | retained_original: The original flag bank and accessors remain paired on Vita. No larger mod flag bank or flag-change events are imported; the Helm fix uses the original bank. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |
| [func_global_asm_80731030](../patches/sound_options_patches.c#L1074) | Relocated temporary flag bank and mod events | retained_original: The original flag bank and accessors remain paired on Vita. No larger mod flag bank or flag-change events are imported; the Helm fix uses the original bank. | Inspected reference C and the Vita build/hook path. No function-specific gameplay test; broad intro checks are not proof of this individual effect. | Revisit if the corresponding desktop feature is added; retain the original behavior for this port. |

### patches/timing_fixes.c

| Replacement | Purpose | Vita disposition and reason | Evidence / limitation | Next action |
|---|---|---|---|---|
| [func_global_asm_8060F730](../patches/timing_fixes.c#L189) | Use the corrected frame deadline and frame interval | equivalent: Instruction patches and dk64_vita_frame_delta implement the desktop completion/deadline changes. | us.vita.toml, platform/vita/timing.cpp, dk64_timing_checks and native intro runs. | Extend timing validation to later minigames and cutscenes. |
| [func_global_asm_8060F254](../patches/timing_fixes.c#L235) | VI deadlines and guarded audio wakeups | equivalent: Native hooks preserve pending presentations until their deadline and replace the delayed audio timer with a message only when the queue is empty. | Host slowed run: 13 audio buffers before vs 202/203 after in 30 s. Native trace passes 1080 audio submissions; underruns remain under graphics load. | Address remaining scheduling/rendering stalls without reinstating duplicate or delayed audio notifications. |
| [func_global_asm_805FE4D4](../patches/timing_fixes.c#L333) | Extended-RDRAM, refresh metadata and camera interpolation commands | retained_original: Vita retains the original display-list prefix. The runtime reports 8 MiB to the unmodified guest allocator, despite reserving 32 MiB; no desktop patch globals or interpolation commands are inserted. Extended GBI still needs implementation before importing this replacement. | librecomp/src/recomp.cpp sets osMemSize to 8 MiB; rt64_fast_unsupported.cpp rejects extended GBI; timing is supplied by native VI/frame helpers. | Reassess when enabling the corresponding frontend, larger guest allocations or extended-GBI feature; retain explicit validation limits. |
